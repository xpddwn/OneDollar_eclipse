<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/cart_bg">

    <!-- TODO: Update blank fragment layout -->
     <RelativeLayout
        android:id="@+id/cart_layout"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_alignParentBottom="true"
        android:background="@color/white" 
        android:paddingLeft="10dp">

        <TextView
            android:id="@+id/cart_my_total"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:gravity="center_vertical"
            android:maxLines="1"
            android:text="My Total:"
            android:textColor="@color/merchandise_content"
            android:textSize="18sp" />

        <TextView
            android:id="@+id/cart_total_num"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_alignParentTop="true"
            android:layout_marginLeft="20dp"
            android:layout_marginStart="20dp"
            android:layout_toEndOf="@+id/cart_my_total"
            android:layout_toRightOf="@+id/cart_my_total"
            android:gravity="center_vertical"
            android:maxLines="1"
            android:text="1.00"
            android:textColor="@color/lucky_name"
            android:textSize="18sp" />

        <Button
            android:id="@+id/cart_check_bt"
            android:layout_width="wrap_content"
            android:layout_height="fill_parent"
            android:layout_alignParentBottom="true"
            android:layout_alignParentEnd="true"
            android:layout_alignParentRight="true"
            android:background="@color/lucky_name"
            android:paddingLeft="15dp"
            android:paddingRight="15dp"
            android:text="Check out"
            android:textColor="@color/white" />
    </RelativeLayout>
    
    <ListView
        android:id="@+id/cart_listview"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@id/cart_layout"
        android:layout_below="@id/title"
        android:fadeScrollbars="true" />

   

</RelativeLayout>